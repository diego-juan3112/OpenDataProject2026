# Pipeline de datos — Alerta Ciudadana (Integrante 1)
# Versiones verificadas en Windows + Python 3.13 (2026-06-30).
# Instalar en un venv:  python -m venv .venv && .venv/Scripts/pip install -r requirements.txt
pandas==3.0.3
geopandas==1.1.4
pyarrow==24.0.0
pyogrio==0.13.0          # backend de I/O geoespacial de GeoPandas 1.x (reemplaza a fiona)
shapely==2.1.2
numpy==2.5.0
requests==2.34.2
tqdm==4.68.3
openpyxl==3.1.5
pytest         # por si alguna fuente futura llega en .xlsx

# EDA / notebooks (Issue #11 — EDA de calidad + nota de sesgo)
matplotlib==3.11.0
seaborn==0.13.2

# Routing externo (Ruta Más Segura — Issues #45–#48)
openrouteservice==2.3.3
polyline==2.0.1
python-dotenv==1.2.2      # lee ORS_API_KEY desde .env (no versionado)
scipy==1.18.0          # EDA/#25 — regresión de tendencia (linregress) + dendrograma jerárquico
scikit-learn==1.9.0    # #27 — K-Means + silhouette para el clustering de perfiles de zona
joblib==1.5.3          # #27 — serializar clusters.joblib
xgboost==3.3.0         # #18 — Gradient Boosting del modelo predictivo

# API ligera (Issue #20 — Integrante 2)
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
httpx>=0.27.0          # TestClient de FastAPI / starlette

